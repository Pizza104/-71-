<!doctype html>
<html>
<head><meta charset="utf-8"><title>Alunos</title></head>
<body>
  <%- include('partials/navbar') %>
  <h1>Alunos</h1>
  <% if (alunos.length === 0) { %>
    <p>Nenhum aluno cadastrado.</p>
  <% } else { %>
    <ul>
      <% alunos.forEach(a => { %>
        <li><%= a.nome %> — <%= a.idade ? a.idade + ' anos' : '' %></li>
      <% }) %>
    </ul>
  <% } %>
</body>
</html>
